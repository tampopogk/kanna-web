# Cut the Kanna marketing site back to a simple, confident page

## Goal

Rebuild `index.html` so it reads as a small, confident product page: the old
pre-refresh site's hero atmosphere (gradient, perspective grid, scanline
overlay) on top of the current site's cleaner layout, one product screenshot, a
plain feature list, the mobile captures, and nothing else. The owner's
complaint is that the current site "reads as obvious AI slop" and that the old
site — "mostly a cool background plus download links" — was better.

## Scope

In:

- Restore the old hero visual treatment: purple → pink → orange gradient, pink
  perspective grid rising from the bottom, fixed scanline overlay, DM Sans +
  DM Mono, the large app icon.
- Single GitHub mark, icon only, top right, with an accessible name.
- One hero image only: `assets/kanna-desktop.avif`.
- A plain text feature list below it, sourced from the marketing repo.
- Mobile section: the three genuine phone captures, plus a locally generated
  App Store QR code and a text link.
- Verify the GitHub Releases download script still resolves.

Out (deleted, deliberately):

- The "Source ↗" nav link and the "View source ↗" button in the final CTA.
- The trust strip (four-capability row).
- The numbered feature cards ("Give autonomy a shape", 01–04).
- The Define/Run/Review/Handoff illustration section.
- The final CTA section, which repeated the download buttons a second time.
- The "Workflows" / "Mobile" nav links.
- Section subtitles that restated their heading.
- The "Genuine product capture" badge and the "Genuine, unaltered capture"
  caption. The standing directive forbids passing fabricated UI off as a
  screenshot; it does not require labelling genuine captures, and all four
  images here are genuine. The badge was one more thing to read.

Untouched, and out of scope: `CNAME`, `privacy/index.html`,
`support/index.html`, the four existing files in `assets/`.

## Copy source of record

Feature copy comes from `/Users/jeremyhale/.kanna/repos/kanna-marketing`
(read-only), specifically `docs/messaging.md`. Its five numbered messages map
one-to-one onto the five rows of the plain feature list, in order: composed
workflows, one durable task, coordinators, agent per stage, away from the desk.
Wording is taken from that doc rather than invented, then shortened.

Two rules from that repo are held here:

- **A Partial claim is not written as Shipped.** Message 4 in the messaging doc
  carries a deliberately qualified clause about provider, model, and
  reasoning-effort settings, because capability inventory §6 rates that
  **Partial**. The site drops that clause entirely and claims only the
  **Shipped** part: that Kanna can run Claude Code, Codex, GitHub Copilot CLI,
  OpenCode, and Antigravity.
- **Kanna is never positioned against Claude Code or any other agent.** The
  agents are named as things Kanna runs, never as competitors.

### Website fix applied from the same doc

`docs/messaging.md` § "Website fixes" lists five corrections to the live page.
Four of them concern sections this rewrite deletes outright (the fabricated
`.kanna/pipelines/default.json` default pipeline, the "Review → PR" human
checkpoint, the commit-agent sentence, and the invented `kanna task start` /
`kanna-cli advance-stage` examples). The fifth applies to copy this page keeps:

> "macOS 14+" → **"Requires macOS 15 or later."**

The old value was contradicted by source: `.bazelrc` sets macOS 15.0 as the
minimum for every shipped Mach-O. It is corrected in both places it appears —
the static `#version-label` text and the template literal the release script
writes into it.

## Directives received mid-task

**Owner, 2026-09-03 — no hero copy.** Verbatim: *"For the old site let's avoid
silly tag lines or hero crap."* No marketing tagline, no aspirational headline,
no descriptive sub-paragraph. The hero is the background treatment, the product
name/icon and the download links; name what the thing is in plain words if a
line is needed at all. This removed the headline "More agents. One clear path
forward.", the gradient slogan "Structured autonomy for coding agents." and the
hero description paragraph. The remaining hero line is the plain naming eyebrow
"macOS app for coding agents". The same rule applies to the feature list: label
the feature, do not sell it.

Note that this overrides the marketing doc's headline and subhead for this
page. `docs/messaging.md` proposes "Structured autonomy for coding agents." as
the headline; the owner's directive is later and more specific, so the page
carries no headline at all.

**Owner, 2026-09-03 — do not publish.** Do not push a branch, open a pull
request, merge, or deploy. This site is served from its main branch, so a merge
is a live deploy. Work stops at local commits on this task's branch; the owner
reviews before anything is published.

**Owner, 2026-09-03 — Kanna Mobile is shipped, with a storefront-neutral URL.**
This supersedes the original task prompt, which said Kanna Mobile was not on
the App Store and asked for a commented placeholder. It also supersedes a
first URL the owner sent, which was the Japan storefront. The URL to use,
verbatim, is `https://apps.apple.com/app/id6802176590`; Apple routes each
visitor to their own country's store. Verified: it returns HTTP 200 and
resolves to `https://apps.apple.com/us/app/kanna-mobile/id6802176590`.
Requirements attached to it: generate the QR locally, commit it as a static
asset, no third-party QR service, no runtime generation, explicit `width` and
`height`, alt text, and a visible text link beside it. The storefront question
is decided and is not an open question.

**Owner directive of 2026-09-03, still binding — no fabricated UI.** Never
present simulated, fabricated, or CSS/HTML-built interfaces as if they were
screenshots. Generated graphics are allowed only as obvious illustrations or
diagrams. The four captures in `assets/` are genuine and are the only product
imagery on the page. This is also why the old site's animated terminal mock was
not lifted: it is a CSS-built fake interface.

**Owner, 2026-09-03 — pricing placement.** Asked rather than assumed, as the
marketing repo instructs. Decision: keep the pricing line, as one plain
sentence beside the App Store QR, which is the one place on the page where the
LAN-versus-relay distinction is relevant. Copy: "LAN access and notifications
are free. Cloud-relay terminal access is $5/month, and free for beta testers."
The free-first framing is deliberate.

**Owner, 2026-09-03 then 2026-09-04 — self-hosted relay: considered, then
deliberately excluded.** This went both ways within the task, so the reasoning
is recorded in full; a later reader should not read the absence as an oversight.

On 2026-09-03 a single cautious clause was permitted: that Kanna is open source
including the relay, *or* that the relay is self-hostable with a link to the
documentation — with setup steps barred from the marketing page, and with no
claim that self-hosting removes the subscription for a stock App Store user
(that depends on entitlement enforcement being off on a relay the operator
controls). The clause "Kanna is open source, relay included." was written and
briefly shipped.

Two problems surfaced while it was in place:

- **No documentation exists to link.** A search of the `kanna` checkout for
  "self-host" across Markdown returned nothing. Inventing a URL was not an
  option, so the self-hostable variant was not usable.
- **Firebase-project constraint.** Running the relay is not the whole story.
  The relay sits behind a Firebase control plane — Firestore for durable state,
  Cloud Functions for pairing and token minting, Firebase auth — so a
  self-hoster needs their own Firebase project, not just the relay service.
  That qualifies what the owner had been told, and it was raised with them.
- **No evidence row.** `docs/research/capability-inventory.md` has no
  self-hosted-relay row, so the claim was not covered by that evidence base.

**Owner decision, 2026-09-04, after seeing the Firebase constraint:** *"Okay.
Tell Kanna TM to hide that feature for now. We'll come back to it."* The
feature is being hidden in the product, so the site must not advertise it. The
clause was removed. The page carries no self-hosted relay claim, no "run your
own relay" phrasing, no link to self-hosted relay documentation, and no mention
of custom relay endpoints. **This is a deliberate exclusion pending a product
change, not an omission.** If the feature is unhidden, this is the decision to
revisit.

Two mentions of "relay" remain and are deliberate. Both describe the *hosted*
cloud relay as a transport and a pricing fact — the mobile terminal caption and
the pricing line — and both are backed as **Shipped** by capability inventory
§8 ("The mobile app can connect directly over the local network or remotely
through the authenticated cloud relay"). Neither implies the operator can run
their own.

**Owner, 2026-09-04 — what survives the removal.** The free-first pricing
framing is unchanged: on your own network traffic stays on the LAN and costs
nothing, notifications are free, and the $5 covers cloud infrastructure for
remote terminal access, free for beta testers. "Open source, bring your own
model" also stays, because it is about the agent CLIs and is independently
true. Since the rewrite deleted the final CTA that used to carry that line, it
is kept as the footer colophon: `KANNA · OPEN SOURCE · BRING YOUR OWN MODEL ·
© TAMPOPO LLC`. The footer is a project-level fact line, so the claim survives
without reintroducing a marketing block or a second GitHub text link.

## Hero treatment decision

The whole page is dark, not just the hero. The old site was dark end to end and
its scanline overlay was `position: fixed` across the entire document; a light
body under a dark hero would have made the scanlines a hero-only gimmick and
split the page into two unrelated halves. All four product captures are of a
dark app, so they sit on a dark page without a frame fighting them.

Lifted from the old file: the `180deg` `#1a0533 → #3d0e62 → #e8357a → #ff8c00`
hero gradient at 0.25 opacity over a near-black ground, the pink perspective
grid (`perspective(400px) rotateX(40deg)`, masked to fade upward), the fixed
repeating-linear-gradient scanline overlay, DM Sans / DM Mono, the letterspaced
mono `KANNA` wordmark with a pink-to-orange gradient fill, the large app icon,
and the `// SECTION LABEL` mono headings.

Not lifted: the animated terminal mock (fabricated UI), the feature "bar"
graphics, and the `Lightweight. Efficient. Fast.` tagline.

Kept from the current site: the layout restraint, the real product icon
artwork, the accessibility and performance work, and the release-fetching
download script.

Restraint over the old file: one centred column, nothing between the wordmark
and the buttons, and a plain naming eyebrow instead of a slogan.

## QR code

`assets/kanna-mobile-qr.svg` encodes `https://apps.apple.com/app/id6802176590`.

- Generated locally with `segno` (version 3, error correction M, 4-module quiet
  zone, 296×296 intrinsic, dark `#12000f` on white), installed into a throwaway
  virtualenv under the ignored `.tmp/`. No third-party QR service is called at
  build time or at runtime.
- **Verified by decoding it back**, not just by generating it: a PNG render was
  read with OpenCV's `QRCodeDetector`, which returned
  `https://apps.apple.com/app/id6802176590` exactly.
- It sits on a white plate because scanners expect dark modules on a light
  field; the page around it is near-black.
- Marked up with explicit `width="296" height="296"`, `loading="lazy"`,
  `decoding="async"`, and alt text naming the destination. The visible text
  link beside it points at the same URL.
- An HTML comment above the block records the same contract, so the next editor
  does not swap it for a runtime generator.

If the URL ever changes, regenerate the SVG the same way and update the `href`,
the alt text, and the comment together.

## Constraints held

- `:focus-visible` outline on the download buttons — kept (and added to the
  GitHub mark).
- Explicit `width`/`height` on every image — kept, values unchanged.
- `loading` / `decoding` attributes, `fetchpriority="high"` on the hero
  capture — kept.
- AVIF assets used where they exist — unchanged.
- `prefers-reduced-motion` block — kept.
- No horizontal scroll at any viewport; `min-width: 320px` on the body.

## Verification

Served locally on port 8137 and driven with headless Chrome over the DevTools
protocol, so the viewport could be emulated properly — plain
`--headless --window-size` clamps its layout viewport to 500px wide, which
produces cropped screenshots that look like overflow bugs and are not.

**Download script — works; the constant was left unchanged.**
`https://api.github.com/repos/jemdiggity/kanna/releases/latest` answers `301`
to `https://api.github.com/repositories/1185799373/releases/latest`, which
resolves to the transferred `tampopogk/kanna` repository. `fetch()` follows
redirects by default and the redirect stays on `api.github.com`, so CORS is
unaffected. In the browser, both buttons receive real URLs —
`https://github.com/tampopogk/kanna/releases/download/v0.2.0/Kanna_0.2.0_arm64.dmg`
and `…_x86_64.dmg` — and the version label reads
`v0.2.0 · Requires macOS 15 or later`. Per the task's instruction to change
nothing if it works, `REPO` still reads `jemdiggity/kanna`.

**No horizontal scroll.** At 1440, 768, 390 and 320 CSS pixels,
`document.documentElement.scrollWidth` equals `window.innerWidth`, and a sweep
of every element in `<body>` found none whose right edge exceeds the viewport.

**Visual.** Desktop (1440×900) and mobile (390 wide, full page) renders
inspected: hero gradient, grid and scanlines present; one screenshot; five
feature rows; three phone captures; QR and store link; footer. The feature list
collapses to one column below 820px and the phones stack below it.

## Done when

`index.html` carries the old hero treatment with no tagline or hero paragraph,
one GitHub icon top right, one product screenshot, a plain feature list sourced
from the marketing repo, the three mobile captures with a locally generated and
decode-verified App Store QR, and none of the deleted sections; the macOS 15
correction is applied; the accessibility and performance attributes are intact;
the download script is verified; the work is committed to this task's branch
and nothing is pushed, merged, or deployed.
