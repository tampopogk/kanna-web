# Cut the Kanna marketing site back to a simple, confident page

## Goal

Rebuild `index.html` so it reads as a small, confident product page: the old
pre-refresh site's hero atmosphere (gradient, perspective grid, scanline
overlay) on top of the current site's cleaner layout, one product screenshot, a
plain feature list, the mobile captures, and nothing else. The owner's
complaint is that the current site "reads as obvious AI slop" and that the old
site — "mostly a cool background plus download links" — was better.

## Scope

In:

- Restore the old hero visual treatment: purple → pink → orange gradient, pink
  perspective grid rising from the bottom, fixed scanline overlay, DM Sans +
  DM Mono, the large app icon.
- Single GitHub mark, icon only, top right, with an accessible name.
- One hero image only: `assets/kanna-desktop.avif`.
- A plain text feature list below it, sourced from the marketing repo.
- Mobile section: the three genuine phone captures, plus a locally generated
  App Store QR code and a text link.
- Verify the GitHub Releases download script still resolves.

Out (deleted, deliberately):

- The "Source ↗" nav link and the "View source ↗" button in the final CTA.
- The trust strip (four-capability row).
- The numbered feature cards ("Give autonomy a shape", 01–04).
- The Define/Run/Review/Handoff illustration section.
- The final CTA section, which repeated the download buttons a second time.
- The "Workflows" / "Mobile" nav links.
- Section subtitles that restated their heading.
- The "Genuine product capture" badge and the "Genuine, unaltered capture"
  caption. The standing directive forbids passing fabricated UI off as a
  screenshot; it does not require labelling genuine captures, and all four
  images here are genuine. The badge was one more thing to read.

Untouched, and out of scope: `CNAME`, `privacy/index.html`,
`support/index.html`, the four existing files in `assets/`.

## Copy source of record

Feature copy comes from `/Users/jeremyhale/.kanna/repos/kanna-marketing`
(read-only), specifically `docs/messaging.md`. Its five numbered messages map
one-to-one onto the five rows of the plain feature list, in order: composed
workflows, one durable task, coordinators, agent per stage, away from the desk.
Wording is taken from that doc rather than invented, then shortened.

Two rules from that repo are held here:

- **A Partial claim is not written as Shipped.** Message 4 in the messaging doc
  carries a deliberately qualified clause about provider, model, and
  reasoning-effort settings, because capability inventory §6 rates that
  **Partial**. The site drops that clause entirely and claims only the
  **Shipped** part: that Kanna can run Claude Code, Codex, GitHub Copilot CLI,
  OpenCode, and Antigravity.
- **Kanna is never positioned against Claude Code or any other agent.** The
  agents are named as things Kanna runs, never as competitors.

### Website fix applied from the same doc

`docs/messaging.md` § "Website fixes" lists five corrections to the live page.
Four of them concern sections this rewrite deletes outright (the fabricated
`.kanna/pipelines/default.json` default pipeline, the "Review → PR" human
checkpoint, the commit-agent sentence, and the invented `kanna task start` /
`kanna-cli advance-stage` examples). The fifth applies to copy this page keeps:

> "macOS 14+" → **"Requires macOS 15 or later."**

The old value was contradicted by source: `.bazelrc` sets macOS 15.0 as the
minimum for every shipped Mach-O. It is corrected in both places it appears —
the static `#version-label` text and the template literal the release script
writes into it.

## Directives received mid-task

**Owner, 2026-09-03 — no hero copy.** Verbatim: *"For the old site let's avoid
silly tag lines or hero crap."* No marketing tagline, no aspirational headline,
no descriptive sub-paragraph. The hero is the background treatment, the product
name/icon and the download links; name what the thing is in plain words if a
line is needed at all. This removed the headline "More agents. One clear path
forward.", the gradient slogan "Structured autonomy for coding agents." and the
hero description paragraph. The remaining hero line is the plain naming eyebrow
"macOS app for coding agents". The same rule applies to the feature list: label
the feature, do not sell it.

Note that this overrides the marketing doc's headline and subhead for this
page. `docs/messaging.md` proposes "Structured autonomy for coding agents." as
the headline; the owner's directive is later and more specific, so the page
carries no headline at all.

**Owner, 2026-09-03 — do not publish.** Do not push a branch, open a pull
request, merge, or deploy. This site is served from its main branch, so a merge
is a live deploy. Work stops at local commits on this task's branch; the owner
reviews before anything is published.

**Owner, 2026-09-03 — Kanna Mobile is shipped, with a storefront-neutral URL.**
This supersedes the original task prompt, which said Kanna Mobile was not on
the App Store and asked for a commented placeholder. It also supersedes a
first URL the owner sent, which was the Japan storefront. The URL to use,
verbatim, is `https://apps.apple.com/app/id6802176590`; Apple routes each
visitor to their own country's store. Verified: it returns HTTP 200 and
resolves to `https://apps.apple.com/us/app/kanna-mobile/id6802176590`.
Requirements attached to it: generate the QR locally, commit it as a static
asset, no third-party QR service, no runtime generation, explicit `width` and
`height`, alt text, and a visible text link beside it. The storefront question
is decided and is not an open question.

**Owner directive of 2026-09-03, still binding — no fabricated UI.** Never
present simulated, fabricated, or CSS/HTML-built interfaces as if they were
screenshots. Generated graphics are allowed only as obvious illustrations or
diagrams. The four captures in `assets/` are genuine and are the only product
imagery on the page. This is also why the old site's animated terminal mock was
not lifted: it is a CSS-built fake interface.

**Owner, 2026-09-03 — pricing placement.** Asked rather than assumed, as the
marketing repo instructs. Decision: keep the pricing line, as one plain
sentence beside the App Store QR, which is the one place on the page where the
LAN-versus-relay distinction is relevant. Copy: "LAN access and notifications
are free. Cloud-relay terminal access is $5/month, and free for beta testers."
The free-first framing is deliberate.

**Owner, 2026-09-03 then 2026-09-04 — self-hosted relay: considered, then
deliberately excluded.** This went both ways within the task, so the reasoning
is recorded in full; a later reader should not read the absence as an oversight.

On 2026-09-03 a single cautious clause was permitted: that Kanna is open source
including the relay, *or* that the relay is self-hostable with a link to the
documentation — with setup steps barred from the marketing page, and with no
claim that self-hosting removes the subscription for a stock App Store user
(that depends on entitlement enforcement being off on a relay the operator
controls). The clause "Kanna is open source, relay included." was written and
briefly shipped.

Two problems surfaced while it was in place:

- **No documentation exists to link.** A search of the `kanna` checkout for
  "self-host" across Markdown returned nothing. Inventing a URL was not an
  option, so the self-hostable variant was not usable.
- **Firebase-project constraint.** Running the relay is not the whole story.
  The relay sits behind a Firebase control plane — Firestore for durable state,
  Cloud Functions for pairing and token minting, Firebase auth — so a
  self-hoster needs their own Firebase project, not just the relay service.
  That qualifies what the owner had been told, and it was raised with them.
- **No evidence row.** `docs/research/capability-inventory.md` has no
  self-hosted-relay row, so the claim was not covered by that evidence base.

**Owner decision, 2026-09-04, after seeing the Firebase constraint:** *"Okay.
Tell Kanna TM to hide that feature for now. We'll come back to it."* The
feature is being hidden in the product, so the site must not advertise it. The
clause was removed. The page carries no self-hosted relay claim, no "run your
own relay" phrasing, no link to self-hosted relay documentation, and no mention
of custom relay endpoints. **This is a deliberate exclusion pending a product
change, not an omission.** If the feature is unhidden, this is the decision to
revisit.

Two mentions of "relay" remain and are deliberate. Both describe the *hosted*
cloud relay as a transport and a pricing fact — the mobile terminal caption and
the pricing line — and both are backed as **Shipped** by capability inventory
§8 ("The mobile app can connect directly over the local network or remotely
through the authenticated cloud relay"). Neither implies the operator can run
their own.

**Owner, 2026-09-04 — what survives the removal.** The free-first pricing
framing is unchanged: on your own network traffic stays on the LAN and costs
nothing, notifications are free, and the $5 covers cloud infrastructure for
remote terminal access, free for beta testers. "Open source, bring your own
model" also stays, because it is about the agent CLIs and is independently
true. Since the rewrite deleted the final CTA that used to carry that line, it
is kept as the footer colophon: `KANNA · OPEN SOURCE · BRING YOUR OWN MODEL ·
© TAMPOPO LLC`. The footer is a project-level fact line, so the claim survives
without reintroducing a marketing block or a second GitHub text link.

## Owner revision round, 2026-09-04

Six items from the owner after seeing the first pass. Five are done; the sixth
is reported below rather than guessed at.

**1. Current icon, dark version — done.** The page had been carrying the *old*
site's icon (a 256-viewBox mark with a sky-gradient plate). The current product
icon is `kanna/apps/desktop/src-tauri/icons/icon.svg`: a 512-viewBox mark, five
rows of gradient bars plus a green status pill, on a white plate. The page now
carries that geometry and those bar gradients verbatim; only the plate and its
hairline are darkened (`#1c1226 → #140c1c`, hairline
`rgba(255,255,255,0.16)`) so it sits on the dark page, which the owner
explicitly allowed. An HTML comment records the provenance and asks the next
editor to keep the bars in sync with the product icon. The wrapper's own
`border-radius`/`overflow`/background were dropped, because this icon draws its
own rounded plate inset within the viewBox.

**2. More screenshots — not delivered; see "Screenshots: what I found" below.**

**3. Composed workflows section — done.** The revision limit is gone; the owner
found it irrelevant and confusing here. The row now describes the mechanism in
plain terms: naming the stages, giving each one an agent, choosing where Kanna
advances by itself and where it stops, and the workflow being a reviewed,
versioned file in the repository.

**4. "One durable task" — cut.** (The replacement row was later widened; see
the workspaces note below.) The owner: it "isn't interesting to humans."
The durable-identity claim is dropped. The genuinely interesting half of
messaging message 2 is kept and promoted to its own row, **Isolated
worktrees**: every task on its own branch in its own worktree, so several
agents can run at once. That is the concrete part a reader can picture.

**5. Paired machines — added.** Push and pull were missing. Previously
cross-machine work was a half-clause inside the mobile row ("Move a task
between reachable Kanna machines"), which buried it. It is now its own row
naming both directions: pair another Mac, push a task to it, pull one back.
Backed **Shipped** by capability inventory §9 ("The desktop can push a local
task to a peer or pull a remote task"). The mobile row is now only about the
phone.

**6. Pricing intent — done, and treated as one requirement with the existing
free-first framing rather than two competing ones.** The store note now leads
with the intent: Kanna aims to be free where it can be, and low cost where
cloud infrastructure is unavoidable; on your own network traffic stays on the
LAN and costs nothing, notifications are free, and the $5/month covers the
cloud infrastructure behind remote terminal access, free for beta testers.

The feature list is now six rows: composed workflows, isolated worktrees,
coordinators, agent per stage, paired machines, away from the desk.

## Screenshots and the capture harness

The owner asked for a capture harness in this repo, with a seed of my own, and
for it to be used for the screenshots. `tools/capture/` is that harness. It
works end to end; what it does not yet produce is a *good* screenshot, and the
reason is worth recording.

### Why not the kanna repo's e2e seed

`kanna/apps/desktop/tests/e2e/seed.sql` seeds three repositories and fourteen
tasks. Its **task titles are good** — "Refactor auth middleware", "Rate
limiting middleware", "Webhook delivery system", "DB schema migration v3". Its
**repository names are not**: `example-app`, `example-api`, `example-docs`,
under `/Users/test`. Repository names are the most prominent text in the Kanna
sidebar, so captures from that seed put the word "example" in front of every
visitor. `tools/capture/seed.sql` is the same idea with homepage names:
`orchard-web`, `orchard-api`, `fieldnotes`, and ten tasks of the same shape.

### What the harness does

- `fixtures.sh` creates real git repositories and a real worktree per task.
  This is not optional: Kanna reconciles its database against the filesystem,
  and the first seeding attempt had every task closed and moved to the `done`
  stage within seconds because the worktrees did not exist. The tasks only
  survive once the branches and worktrees are real.
- `seed.sh` substitutes the fixture root into `seed.sql` and refuses to run
  against `kanna-v2.db`.
- `capture.mjs` drives the app over the WebDriver server from
  `tauri-plugin-webdriver` and asks the app for its own screenshot.
- `convert.sh` produces AVIF and prints intrinsic sizes for the markup.

A **debug build is required**: `apps/desktop/src-tauri/src/lib.rs` initialises
the WebDriver plugin under `#[cfg(debug_assertions)]`, so the installed release
app cannot be driven.

### Isolation, and what it was isolated from

The owner's live instance is **Kanna Staging.app**, and it is running this very
task, so a second instance had to be provably separate. It was: its own
database (`kanna-capture.db`, with `kd` refusing production `kanna-v2.db`), its
own `KANNA_DAEMON_DIR`, and its own ports (1520 / 4555 / 9180 / 48220 / 8181 /
4655, all verified free first). The build was run under a `Cargo.lock`
checksum guard because that checkout had uncommitted changes; the checksum was
unchanged afterwards. Shutdown used `kd dev down` and then a kill of the single
daemon PID under this task's build path. **`kd dev down --kill-daemon` was
deliberately not used**: it reaps daemons, and four other tasks' daemons were
live on this machine. Kanna Staging was confirmed still running afterwards.

### What came out, and why it is not on the page

The seeded task list renders correctly and reads well: three plausible
repositories, stage groupings, real task titles. But the right-hand pane is
empty, because an attached agent session is what fills it and no agent is
running. The existing hero capture has a live terminal in that pane; the new
one has a blank half. Adding a half-empty screenshot to a page that was just
cut back for looking padded would make the page worse, so **no new capture was
wired in**.

Two automation limits found on the way, recorded so the next attempt does not
rediscover them: the command palette is **⇧⌘P**, not ⌘K, and neither synthetic
`KeyboardEvent`s nor the WebDriver actions API opened it — its shortcut context
wants main-window focus that injected keys did not reach. `⌘/` opens the
keyboard-shortcuts overlay, which must be dismissed before capturing.

**What would finish it:** something real in that right-hand pane. Either an
agent session actually running against a fixture worktree, or a shell tab with
genuine output. Both are real UI and neither is fabricated; both need either
UI automation that reaches the main window or a person driving the dev
instance. Referred to the owner rather than solved by faking pane content,
which the standing directive forbids outright.

## Hero treatment decision

The whole page is dark, not just the hero. The old site was dark end to end and
its scanline overlay was `position: fixed` across the entire document; a light
body under a dark hero would have made the scanlines a hero-only gimmick and
split the page into two unrelated halves. All four product captures are of a
dark app, so they sit on a dark page without a frame fighting them.

Lifted from the old file: the `180deg` `#1a0533 → #3d0e62 → #e8357a → #ff8c00`
hero gradient at 0.25 opacity over a near-black ground, the pink perspective
grid (`perspective(400px) rotateX(40deg)`, masked to fade upward), the fixed
repeating-linear-gradient scanline overlay, DM Sans / DM Mono, the letterspaced
mono `KANNA` wordmark with a pink-to-orange gradient fill, the large app icon,
and the `// SECTION LABEL` mono headings.

Not lifted: the animated terminal mock (fabricated UI), the feature "bar"
graphics, and the `Lightweight. Efficient. Fast.` tagline.

Kept from the current site: the layout restraint, the real product icon
artwork, the accessibility and performance work, and the release-fetching
download script.

Restraint over the old file: one centred column, nothing between the wordmark
and the buttons, and a plain naming eyebrow instead of a slogan.

## QR code

`assets/kanna-mobile-qr.svg` encodes `https://apps.apple.com/app/id6802176590`.

- Generated locally with `segno` (version 3, error correction M, 4-module quiet
  zone, 296×296 intrinsic, dark `#12000f` on white), installed into a throwaway
  virtualenv under the ignored `.tmp/`. No third-party QR service is called at
  build time or at runtime.
- **Verified by decoding it back**, not just by generating it: a PNG render was
  read with OpenCV's `QRCodeDetector`, which returned
  `https://apps.apple.com/app/id6802176590` exactly.
- It sits on a white plate because scanners expect dark modules on a light
  field; the page around it is near-black.
- Marked up with explicit `width="296" height="296"`, `loading="lazy"`,
  `decoding="async"`, and alt text naming the destination. The visible text
  link beside it points at the same URL.
- An HTML comment above the block records the same contract, so the next editor
  does not swap it for a runtime generator.

If the URL ever changes, regenerate the SVG the same way and update the `href`,
the alt text, and the comment together.

## Constraints held

- `:focus-visible` outline on the download buttons — kept (and added to the
  GitHub mark).
- Explicit `width`/`height` on every image — kept, values unchanged.
- `loading` / `decoding` attributes, `fetchpriority="high"` on the hero
  capture — kept.
- AVIF assets used where they exist — unchanged.
- `prefers-reduced-motion` block — kept.
- No horizontal scroll at any viewport; `min-width: 320px` on the body.

## Verification

Served locally on port 8137 and driven with headless Chrome over the DevTools
protocol, so the viewport could be emulated properly — plain
`--headless --window-size` clamps its layout viewport to 500px wide, which
produces cropped screenshots that look like overflow bugs and are not.

**Download script — works; the constant was left unchanged.**
`https://api.github.com/repos/jemdiggity/kanna/releases/latest` answers `301`
to `https://api.github.com/repositories/1185799373/releases/latest`, which
resolves to the transferred `tampopogk/kanna` repository. `fetch()` follows
redirects by default and the redirect stays on `api.github.com`, so CORS is
unaffected. In the browser, both buttons receive real URLs —
`https://github.com/tampopogk/kanna/releases/download/v0.2.0/Kanna_0.2.0_arm64.dmg`
and `…_x86_64.dmg` — and the version label reads
`v0.2.0 · Requires macOS 15 or later`. Per the task's instruction to change
nothing if it works, `REPO` still reads `jemdiggity/kanna`.

**No horizontal scroll.** At 1440, 768, 390 and 320 CSS pixels,
`document.documentElement.scrollWidth` equals `window.innerWidth`, and a sweep
of every element in `<body>` found none whose right edge exceeds the viewport.

**Visual.** Desktop (1440×900) and mobile (390 wide, full page) renders
inspected: hero gradient, grid and scanlines present; one screenshot; five
feature rows; three phone captures; QR and store link; footer. The feature list
collapses to one column below 820px and the phones stack below it.

## Done when

`index.html` carries the old hero treatment with no tagline or hero paragraph,
one GitHub icon top right, one product screenshot, a plain feature list sourced
from the marketing repo, the three mobile captures with a locally generated and
decode-verified App Store QR, and none of the deleted sections; the macOS 15
correction is applied; the accessibility and performance attributes are intact;
the download script is verified; the work is committed to this task's branch
and nothing is pushed, merged, or deployed.

## Later owner directives, 2026-09-04

**Pricing correction.** Notifications work over WAN for free, not only on the
LAN; using the agent terminal through the relay needs cloud and so is not free.
The earlier wording tied notifications to the LAN clause and was wrong. Current
copy: notifications are free anywhere, on your own network or away from it; the
agent terminal is free over the LAN; reaching it from outside runs through the
cloud relay, and that is the $5/month, free for beta testers.

**"Isolated worktrees" became "Isolated workspaces".** The owner's framing: a
workspace is the git worktree *plus* the ports a task needs for dev servers,
Firebase emulation, and other services. Kanna puts the port values in the
environment and runs startup and teardown scripts, so several workspaces can
each run a web app, a mobile app, an emulator and other backends at once. The
row now says that. Corroborated by the seed schema's `task_port` table
(`port_env`, e.g. `KANNA_DEV_PORT`) and by `.kanna/config.json` declaring
`ports` that Kanna reserves per task.

## Owner round, 2026-09-04 (later)

**I ran the capture against the production data directory. That was wrong.**
`kd dev up` was run from the main `kanna` checkout with `KANNA_DB_PATH` set to a
scratch path, but the app resolves its database *by name* into its own
Application Support directory — and for that build that directory is
`~/Library/Application Support/build.kanna/`, which is **production**, not a
dev sandbox. `KANNA_DB_PATH` misled me: `kd` did create a file there, so two
`kanna-capture.db` files existed and I reasoned from the wrong one.

What was actually done to production: three files were created,
`kanna-capture.db`, `-wal` and `-shm`. Nothing else was written. The production
database `kanna-v2.db` was not touched — its mtime stayed at 31 Aug, before
this session. The three files have been deleted. Staging
(`build.kanna.staging/`) was never involved; it is where the owner's live
instance runs, and it stayed up throughout.

The lesson, now in the harness README and in the follow-up task's prompt:
**verify which database the app actually opened before seeding**, via
`window.__KANNA_E2E__.dbName` plus `lsof` for the full path, and confirm that
path is inside a dev sandbox.

**Captures move to their own task in the kanna repo.** Per the owner, this work
should not run instances from the kanna-web task at all. Kanna task
`199bdc3b` ("Marketing screen grabs from a seeded dev instance") was created in
the `kanna` repo on the `no-review` workflow. It runs a dev instance from its
own worktree, uses the harness copied to
`/Users/jeremyhale/.kanna/captures/harness/`, and writes PNGs, AVIFs and a
`MANIFEST.md` with intrinsic dimensions to `/Users/jeremyhale/.kanna/captures/out/`,
which both tasks can reach. Its prompt carries the production-directory
mistake, the never-`--kill-daemon` rule, the fixtures-before-seed ordering, and
the requirement that the right-hand pane hold real agent or shell output rather
than being faked.

**"Agent per stage" became "Any agent, any stage".** The owner: it is not just
an agent per stage but a workflow per stage, using whatever agent you want.
Checked against a real definition (`specialized-reviewers.json`): each stage
carries its own `agent`, its own `prompt`, its own `policy.transition`, and its
own `post` action with its own agent. So a stage is composed, not a slot, and
the row now says that.

**Hero copy adapted from the owner's Reddit title.** The title was retrieved
from Reddit's oEmbed endpoint (the HTML and `.json` endpoints are blocked;
oEmbed is not), and confirms author `jemdiggity`:

> Looking for beta testers for Kanna. Build with Opus, Review with Codex, Have
> Fable manage it all and send you a text when it needs something. MacOS & iOS
> app that provides structured workflows for agent CLIs.

Adapted as: an eyebrow of "macOS & iOS · structured workflows for agent CLIs",
and a pitch line under the wordmark, "Build with Opus. Review with Codex. Have
Fable manage it all and ping your phone when it needs something." The page
title and meta description follow the same descriptor.

Two deliberate deviations from the original wording:

- "send you a text" became "ping your phone". Kanna sends push notifications,
  not SMS; "text" would read as SMS and overstate it.
- This reintroduces one line under the wordmark, which the 2026-09-03 "no tag
  lines or hero crap" directive had removed. The owner asked for their title to
  be adapted as ad copy, which is later and specific. It is also concrete
  rather than aspirational — naming what you actually do with the product —
  which is what that earlier directive was objecting to the absence of.

**One accuracy caveat to flag.** "Build with Opus … Fable manage it all" names
specific models per stage. Choosing a *provider* per stage is **Shipped**
(capability inventory §6), but selecting a *model* is rated **Partial** there —
"subject to what each provider CLI supports". This is the owner's own public
copy about their own product, so it stands, but it leans on the Partial half
and should get an inventory row before it is leaned on further.

## Mobile and paired-machines copy, 2026-09-04

**Dropped from "Paired machines":** "Work follows the machine that should be
doing it." Owner's call. The row is now just the mechanism — pair another Mac,
push a task to it, pull one back.

**"Away from the desk" rewritten** along the owner's lines: an agent notifies
you when it finishes or needs a decision, tapping the notification opens that
task, and from the phone you can talk to a running agent in a live terminal,
read its files and diffs, send input, and advance a stage. Every clause there
is **Shipped** in capability inventory §8: task-linked push with "tapping opens
that task", the live terminal over LAN or relay, and "inspect task/agent state
and files or diffs, send input to the live task, and advance an eligible
workflow stage".

**Two items from the owner's list are NOT on the page, pending verification.**
The owner also named the **dev server** and a **visual companion** as viewable
on Kanna Mobile. Neither appears in capability inventory §8, and neither could
be confirmed from the local `kanna` checkout — that checkout sits at `v0.0.51`
(`d74d0836`), older than the `K@e3610b45` snapshot the inventory was built
from, so files the inventory cites (`screens/taskActionMenu.ts`, the mobile
section of `docs/dev/product-behavior.md`) do not exist there. `grep` found no
"visual companion" anywhere in it.

**Resolved the same day: the owner updated the checkout and both check out.**
The stale checkout was the whole problem — its `HEAD` sits on the `v0.0.51`
release commit, but `origin/main` is now `b9208335` (2026-09-03), a descendant
of the inventory's `e3610b45`. Reading `origin/main` directly, without touching
that working tree:

- **Dev server on mobile — confirmed.**
  `apps/mobile/src/screens/TaskPreviewModal.tsx` renders a `react-native-webview`
  against a task's ports, driven by `openTaskPreview(taskId, portName)`. That
  method is declared on the real client at
  `apps/mobile/src/lib/api/client.ts:203` and implemented in
  `apps/mobile/src/lib/transports/lanTransport.ts:246` — not test-only. Ports
  arrive as `TaskPort { name, port }` / `TaskPreviewPort { listening }`, with
  `DEV_PORT` the worked example. So a task's own dev server really does open in
  a preview on the phone, which also ties this row to the "Isolated workspaces"
  row's per-task ports.
- **"Visual companion" — read as that same preview.** The only "companion" code
  is `companion_bridge.rs` / `desktopCompanionBridge.ts`, which is the desktop
  side of remote terminals rather than a mobile view. The user-visible thing the
  owner was describing is the webview preview above, so the page says "open the
  task's own dev server in a preview" rather than borrowing an internal term.
- **Files and diffs — confirmed** on current main too:
  `RepoExplorer.tsx`, `TaskFilePreview.tsx`, `TaskDiffPreview.tsx`.

**"send it input" dropped** from the row at the owner's request: talking to a
running agent in a live terminal already covers it.

## The harness was mislabelling its own screenshots, 2026-09-04

Asked whether the harness was actually taking screenshots, I checked instead of
answering from memory, and it was half true. It did capture real pixels from
the app's own WebDriver `/screenshot`. But the two files it produced,
`tasks.png` and `command-palette.png`, were **byte-identical** — same SHA1
`3e93dc48…`. The palette never opened, the setup step failed silently, and the
harness cheerfully logged "captured … (Command palette open over the task
view)" over a duplicate of the previous shot.

That is worse than capturing nothing: it hands the next person a mislabelled
image and a log that says it worked.

Two guards added to `capture.mjs`, and it now exits non-zero when either trips:

- **`verify` per shot.** A boolean expression evaluated in the page after
  setup. If the shot never reached the state it claims, it is refused and
  named, not captured. The palette's verify is
  `/type a command/i.test(document.body.innerText)`.
- **Duplicate detection.** Every screenshot is SHA1'd; a shot identical to an
  earlier one is refused, because two shots showing the same pixels are the
  same state whatever the labels say.

Both were proved against a stub WebDriver server that always returns the same
image and lets the verify result be chosen:

| case | result |
|---|---|
| verify returns false | both shots refused by name, exit 1, nothing written |
| verify passes, images identical | first written, second refused as "identical to tasks", exit 1 |

The stub also caught a regression the edit had introduced — an earlier
replacement had deleted the `DISMISS_OVERLAYS` constant while leaving its call
site, so the script died with `DISMISS_OVERLAYS is not defined`. Restored.

The shared copy at `/Users/jeremyhale/.kanna/captures/harness/` is updated, and
task `199bdc3b` was told, since it had already copied the old version.

## Screenshots landed, 2026-09-04

Task `199bdc3b` delivered four genuine captures to
`/Users/jeremyhale/.kanna/captures/out/`, all 2400x1600, all verified distinct
(four different SHA1s) and reviewed for private content before use. They come
from a debug build run in that task's own worktree against a database inside
that worktree — not production `build.kanna/`, not staging.

Three are on the page:

- `kanna-tasks.avif` — **replaces the hero capture.** The old
  `kanna-desktop.avif` showed the owner's real task list: private repository
  names and in-progress titles that read as internal notes. It has been deleted
  from `assets/`. The replacement shows fixture repositories with a live agent
  session mid-run in the right-hand pane.
- `kanna-command-palette.avif` and `kanna-diff.avif` — a two-up row after the
  feature list. The palette shot happens to show Push to Machine, Pair Machine,
  Merge Master and Task Manager, which is the "Paired machines" and
  "Coordinators" rows made visible.
- `kanna-commit-graph.avif` was **not** used; the capture task rated it weakest
  and it is sparse, the fixture repo having only six commits.

### Why my own attempt got a blank pane

Diagnosed by the capture task, and worth recording because it is not obvious:
**macOS suspends `requestAnimationFrame` for an occluded webview, and xterm only
paints on a frame.** The window must be visible and focused or every terminal
pane screenshots empty while the rest of the UI renders normally. Nothing to do
with seeding.

It also found three real defects in the seed I wrote:

1. `agent_type` is the session *kind* (`pty` / `agent`), not the provider name.
   I had put `claude` / `codex` / `copilot` / `opencode` in it, and
   `TerminalTabs.vue` renders nothing for an unrecognised value — so there was
   no terminal in the pane at all.
2. It used a `tags` column that no longer exists.
3. It used `in_progress` where the app expects `in progress`.

A task terminal is also attach-only — the desktop never spawns an agent for an
existing task — so a seeded task needs a daemon session created under its id
first. The corrected harness is at
`/Users/jeremyhale/.kanna/captures/out/harness/`, with `wd.mjs`, `prepare.mjs`,
`nudge.mjs` and `seed-fixup.sql` added. **That copy supersedes
`/Users/jeremyhale/.kanna/captures/harness/` and `tools/capture/seed.sql` here,
both of which still carry the three bugs above.**

### One thing to look at

Both desktop captures show `bypass permissions on` in the agent terminal's
status line. It is genuine and it is what the fixture session ran under, but it
advertises a permissive mode on the homepage. Flagged for the owner rather than
edited, since editing pixels is exactly what the no-fabrication rule forbids;
the fix, if wanted, is to re-capture with a different permission mode.

## One carousel, and the agent control surface, 2026-09-04

**All desktop screenshots are now a single carousel.** It replaces both the
standalone hero still and the two-up stills row the previous round added. Four
slides: tasks, command palette, diff, commit graph — `kanna-commit-graph.avif`
earns a place here that it did not as a standalone still, because a slide costs
no page weight and it is the only view of the commit graph.

Built on native CSS scroll-snap rather than a library, so it swipes and scrolls
with no JavaScript at all; the script only adds arrows and dots on top.
Verified with page scripts disabled: all four slides present, the track still
scrollable, no page overflow. Verified with scripts on, at 1440 and 390: four
dots built, arrows advance the track, `aria-current` follows the slide, and the
prev/next buttons disable at the ends. Keyboard left/right arrows work on the
focused track, and `prefers-reduced-motion` turns off the smooth scroll.

The three phone captures were left in the Kanna Mobile section rather than
folded into this carousel. They are a different aspect ratio (1179x2556 against
2400x1600), a different product surface, and they sit beside the App Store QR
and the pricing line, which is the point of that section. If "one carousel"
was meant to cover those too, it is a small change — say so.

**New row: "Agents drive Kanna".** Copy about the MCP and CLI surfaces, per the
owner. Both are **Shipped** in capability inventory §10: `kanna-cli` with
`info`, `guide`, `stage-complete`, `repo`, `task`, `tool`, `machine`; and
`kanna_*` MCP tools covering task, workflow, repository, machine, input and
notification operations. The row names what an agent inside a task can do —
open follow-up tasks, send input to another task, notify a phone, record its
own stage result — and ties it back to the coordinators row, since that is the
mechanism behind them. Every one of those was exercised from this session:
`kanna_create_task` created task `199bdc3b`, `kanna_send_task_input` messaged
it, and `kanna_complete_stage` recorded this stage.

Per `docs/messaging.md` § "Website fixes", no invented commands appear: the
page names the surfaces, not `kanna task start`, `kanna mcp ...`,
`kanna-cli advance-stage` or `kanna-cli run-agent`, all of which that document
flags as not existing.

## Zoom, mobile strip, agent variety, 2026-09-04

**Tap to zoom on the carousel.** Each slide image is wrapped in a real
`<button>`, so it works by keyboard and announces itself, and opens a lightbox
overlay. Escape or a backdrop click closes it, body scroll locks while open,
and focus moves to the close button and back to the trigger afterwards.

Two bugs found while verifying it, both worth recording because the first
nearly shipped:

- **The lightbox was covering the entire page at all times.** `.lightbox` sets
  `display: grid`, and an author rule beats the user-agent `[hidden]` rule at
  equal specificity, so the `hidden` attribute did nothing. My functional test
  passed anyway, because it asserted on the `hidden` *property* rather than on
  computed style — the overlay was "hidden" by that measure while visibly
  greying out the whole site. A screenshot caught it. Fixed with an explicit
  `.lightbox[hidden] { display: none; }`, and the test now asserts
  `getComputedStyle(...).display` at rest, when open, and after close.
- **Focus was stranded on the hidden close button.** Restoring to
  `document.activeElement` captured at open time is wrong: a click does not
  reliably focus a button, so the remembered element was `<body>`, and
  `body.focus()` is a no-op that left focus on the close button after it was
  hidden. Now the trigger button itself is remembered and restored, verified by
  asserting `document.activeElement === the zoom button` after Escape.

**Phone captures no longer stack on small screens.** Stacked vertically they ran
to about three full screens. Below 820px they are now a horizontal scroll-snap
strip, each phone at 78% of the width so the next one peeks in and the strip
reads as swipeable without needing arrows or dots. Verified at 390px: the
container is a flex row, scrolls internally, and the page itself still has no
horizontal overflow.

Note on measuring overflow with a carousel on the page: counting every element
whose right edge exceeds the viewport now reports false positives, because
off-screen slides legitimately sit to the right inside a scroll container. The
checks exclude anything inside `.track`, `.phones` or `.lightbox` and rely on
`document.documentElement.scrollWidth === innerWidth` as the real test.

**Agent variety in the seed.** The owner wants the desktop screenshots to show
a mix of agents rather than one provider everywhere. `tools/capture/seed.sql`
now spreads all five two apiece — Claude, Codex, Copilot, OpenCode,
Antigravity — across the three repositories and the different stages.

It also sets the columns correctly at source rather than relying on a follow-up
fixup: `agent_provider` carries the provider and `agent_type` is the session
kind, always `'pty'`. `seed-fixup.sql` is deleted, since there is nothing left
to fix up. The rest of `tools/capture/` is synced from the corrected harness
the capture task produced.

**"Failed to reconnect to existing session".** The owner saw a number of these
during the capture run. Source:
`apps/desktop/src/composables/terminalSessionRecovery.ts:176`,
`formatAttachFailureMessage` — it writes that text in red *into the terminal
pane*, so it can land inside a screenshot. The capture task's own
`terminal-recovery` log shows its daemon restarting mid-run (pid 52645 →
"shutdown: service stopped cleanly" → pid 41333), and a task terminal is
attach-only, so sessions created under the old daemon are orphaned and the pane
prints the error instead of attaching. Relayed to task `199bdc3b` with three
asks: keep the daemon up across prepare and capture, assert no "Failed to
reconnect" text in the pane before each shot, and re-check the four delivered
captures. `kanna-tasks` and `kanna-command-palette` were inspected here and are
clean.

**File explorer** requested from task `199bdc3b` as a fifth carousel slide, to
match at 2400x1600 with PNG, AVIF and a MANIFEST row. Not yet delivered.

## Owner-supplied captures for file explorer and commit graph, 2026-09-04

The owner shot these two themselves and they are better than the harness could
manage — the commit graph in particular, because the fixture repository has six
commits while the real one has a dense branch history worth showing.

**A first pair was rejected on privacy grounds and re-shot.** The 12:05 pair had
the sidebar expanded, and it showed other repositories' task titles in full:
`fennec-mono` ("Grade-2 RL transfer on Mac Studio", "Human PCB layout editor +
RL demonstrations") and `aminiti-2` ("Fix ami MCP: one long-running tool call
blocks all others", "ami: serial lease goes stale when monitor app relaunches",
"Refine setup wizard firmware gate"). `aminiti-2` maps to a separate
organisation, so publishing it would have put a third party's internal work on
a public homepage. That was raised rather than cropped or published, and the
owner re-shot at 12:15 with the other repositories collapsed to bare names.

The 12:15 pair is what shipped, as `kanna-file-explorer.avif` and
`kanna-commit-graph.avif` (2786x1626, `avifenc --min 20 --max 34 --speed 4`, to
match the existing set). The owner's shot supersedes the capture task's own
`kanna-file-explorer` and `kanna-commit-graph`.

Still visible in them, accepted by the owner in re-shooting rather than
overlooked: the collapsed repository names `fennec-mono` and `aminiti-2`, the
owner's own name on the commit rows, branch names and PR numbers from the
public `tampopogk/kanna` repository, and `bypass permissions on` in the agent
status line.

**The carousel now mixes two capture sizes** — 2400x1600 from the harness and
2786x1626 from the owner. Slides are pinned to a `16 / 10` box with
`object-fit: contain`, so the carousel keeps one height instead of jumping
between slides, and every image is shown whole rather than one set being
cropped to match the other. Verified: all five slide images render at an
identical height at both 1440px and 390px.

Carousel is five slides now: tasks, command palette, diff, file explorer,
commit graph.
