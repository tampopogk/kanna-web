# Blocking tasks and subtasks copy

## Goal

Add homepage copy explaining that Kanna supports blocking tasks and subtasks.

## Scope and constraints

- Update the existing homepage feature copy in `index.html` using accurate Kanna terminology.
- Keep the static page structure and styling unchanged.
- Do not change support, privacy, product behavior, imagery, or deployment.

## User revision (2026-09-04)

The user said, "it would be better to say that kanna automatically starts the task once blocking tasks have reach PR stage. or something like that." The copy now makes the automatic start explicit. It retains "finished" rather than the suggested PR-stage threshold because the current Kanna tool catalog says blockers must finish and the checked implementation resolves a blocker only when its task is closed.

The user then refined the emphasis: "or better yet, that you can stack up dependent tasks that will start once their deps are unblocked..." This supersedes the previous sentence-level direction. The final copy describes stacking dependent tasks behind blockers and Kanna starting each task automatically as soon as its dependencies clear, while retaining the separate subtask-parent relationship.

The user followed with, "maybe give an example as well." The final copy includes a compact dependency chain: a UI task queued behind an API task, followed by an integration task blocked by both.

The user then directed, "separate subtask and blockers." The homepage now presents Subtasks and Task blockers as separate feature entries; the dependency example and automatic-start behavior belong to Task blockers.

The user added, "also users don't care if it's one catalog or not." The adjacent Agents drive Kanna entry no longer describes MCP and CLI as two views of one catalog; it leads with the actions agents can take and mentions MCP or CLI only as the mechanism.

The user then said, "give the specialized reviwer as an example of substasks. the blockers copy is confusing. stack up dependent tasks." The specialized-review fan-out now illustrates Subtasks instead of appearing under Coordinators. Task blockers is reduced to the direct benefit: stack dependent tasks, and Kanna starts each when everything it depends on is done; the API/UI/integration example and dependency jargon are removed.

The user clarified, "keep the previous example for the depdendent tasks." The API → UI → integration dependency example is restored under Task blockers, while the simpler "Stack up dependent tasks" framing remains.

The user directed, "don't say \"send input\" just say \"talk to\" other tasks." The Agents drive Kanna entry now uses the user-facing phrase "talk to other tasks."

The user added, "the task manager coordinates across machines..." The Coordinators entry now distinguishes the long-running task manager's cross-machine scope from merge roles coordinating completed changes within each repository.

The user then said, "don't need to mention the merge master agent." The merge role is removed from the homepage entry, which is renamed Task manager and describes only its cross-machine coordination.

The user directed, "say \"work across machines\" instead of paired machines. it's ok to use \"paired\" as the action of authorizing machines." The feature label is now Work across machines; the body retains "Pair another Mac" as the authorization action.

The user said, "coordinates work is better, but I prefer babysits tasks." The Task manager copy now says it "babysits tasks across machines" instead of "coordinates work across machines."

The user said, "the task manager also works really greate as a single point of contact." The Task manager entry now adds that it gives the user a single point of contact for tasks across machines.

The user directed, "we don't need to say that tapping the notication opens the task as it's obvious." That interaction explanation is removed from Away from the desk; the copy moves directly from notifications to mobile controls.

## Done when

The homepage clearly describes both task blocking and subtasks, the resulting HTML passes the repository's practical checks, and the task spec matches the delivered change.
